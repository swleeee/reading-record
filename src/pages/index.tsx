import React from 'react';

import { BestCommentary, MainLayout } from '@/components';

const root = () => {
  return (
    <MainLayout>
      <BestCommentary />
    </MainLayout>
  );
};

export default root;
