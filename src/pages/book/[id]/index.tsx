import React from 'react';

import { BookDetail, MainLayout } from '@/components';

const root = () => {
  return (
    <MainLayout>
      <BookDetail />
    </MainLayout>
  );
};

export default root;
